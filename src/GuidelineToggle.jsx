import { memo } from 'react';

const GuidelineToggle = () => {
    return (
        <section className='guideline-toggle'>
            <span></span>
            <span></span>
        </section>
    );
};

export default memo(GuidelineToggle);